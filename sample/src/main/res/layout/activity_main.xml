<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="rayman.sample.MainActivity">

    <com.tokenautocomplete.InputCompletionView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusableInTouchMode="true"
        android:hint="Pleasse input you content"
        android:imeOptions="actionDone"
        android:inputType="text|textNoSuggestions|textMultiLine"
        android:lineSpacingExtra="5dp"
        android:nextFocusDown="@+id/editText"
        android:textColor="@android:color/darker_gray"
        android:textSize="19sp"
        app:tokenBackground="@drawable/token_background"
        app:tokenEllipsize="end"
        app:tokenMaxLines="1"
        app:tokenPaddingBottom="2dp"
        app:tokenPaddingLeft="5dp"
        app:tokenPaddingRight="5dp"
        app:tokenPaddingTop="2dp"
        app:tokenTextColor="#ff9b2d" />
</RelativeLayout>
